# Production Environment Variables

# Database configuration - use connection string with proper credentials and pooling
DATABASE_URL=${DATABASE_URL}

# OneKitty API settings - ensure these are secure production values
ONE_KITTY_API_KEY=${ONE_KITTY_API_KEY}
ONE_KITTY_API_HOST=https://api.onekitty.io
ONE_KITTY_CHANNEL_CODES=${ONE_KITTY_CHANNEL_CODES}

# Base application URL (used for callbacks)
VERCEL_URL=https://vote-seven-lovat.vercel.app

# Callback URL for payment notifications
ONEKITTY_CALLBACK_URL=https://vote-seven-lovat.vercel.app/api/payments/callback

# Application settings
NEXT_PUBLIC_TEST_MODE=false
NODE_ENV=production

# Performance settings
VERCEL_EDGE_CONFIG=enabled
VERCEL_CDN_CACHE_CONTROL="public, max-age=31536000, immutable"
VERCEL_ASSET_CACHE_TTL=31536000

# Logging settings
LOG_LEVEL=error

# Feature flags
FEATURE_DIRECT_PAYMENT=true
